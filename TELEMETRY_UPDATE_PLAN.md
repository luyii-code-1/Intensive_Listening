# 数据遥测与自动更新检查规划

本文件定义后续实现边界；当前版本不发送遥测，也不执行联网更新检查。

## 匿名使用情况分析

- **用途**：了解播放器、课程制作、ASR 和 MCP 功能的使用频率与失败率，指导兼容性和性能改进。
- **采集单位**：在本机按日汇总事件计数及耗时区间；上传的是汇总值，不生成持久设备 ID 或用户 ID。
- **候选事件**：应用启动、打开课程、开始播放、使用单句循环、启动或完成 ASR、工程校验成功或失败、MCP 会话开始或结束。事件仅附应用版本、操作系统大版本与粗粒度耗时区间。
- **排除字段**：音频、字幕、题目、答案、文件名、路径、API 地址、密钥、MCP 令牌、日志正文、精确时间点和任何文档内容。
- **授权**：首次提供明确的可选开关，默认关闭；用户可以在设置中查看采集字段、撤回授权并清除待发送汇总。调试日志与遥测授权彼此独立。
- **传输与保留**：后续确定服务端时再定义 HTTPS 接口、短期原始请求保留期、汇总保留期和删除流程。离线时不影响制课或播放；队列需有体积上限。
- **验收**：关闭授权时无遥测网络请求；审阅一份真实上报负载，确认不含课程内容或可识别字段；服务端只输出聚合统计。

## 自动更新检查

- **范围**：只检查新版本并向用户展示版本号、发布日期及发布说明。下载和安装是后续独立决策。
- **分发物**：Windows 安装版使用正式安装程序，单文件版使用正式单文件可执行程序；不生成或消费 ZIP 更新包。
- **检查协议**：应用启动后延迟检查，随后最多每 24 小时检查一次，并提供手动“检查更新”。HTTPS 发布清单包含版本、渠道、最低数据格式版本、发布日期、发布说明地址、分发物地址、大小与 SHA-256。
- **真实性**：发布清单由固定公钥验证签名；下载前还需校验分发物哈希，并核对 Windows 签名。旧版本、不同渠道或不兼容数据格式不进入更新提示。
- **交互**：后台检查不阻断播放和转写。发现新版后显示一次提示，可查看说明或打开官方下载页；失败仅写入调试日志，不反复弹窗。
- **验收**：离线、超时、清单损坏、签名无效、降级、同版本和不同渠道都不会误报新版；检查阶段不修改安装目录和用户数据。

## 实施前需要确定

1. 遥测服务的部署位置、隐私政策、授权文案和数据保留期限。
2. 发布渠道与正式下载域名、签名公钥管理和 Windows 代码签名流程。
3. 是否对安装版与单文件版采用相同的更新提示节奏。

## 设计依据

- [Microsoft：Windows 应用代码签名选项](https://learn.microsoft.com/en-us/windows/apps/package-and-deploy/code-signing-options)
- [OWASP ASVS 4.0：软件更新安全要求](https://wiki.owasp.org/images/d/d4/OWASP_Application_Security_Verification_Standard_4.0-en.pdf)
